import { identity } from '@quenk/preconditions';

export const date = identity;
export const name = identity;
export const range = (_min:number, _max:number)=>identity;
export const email = identity;
export const tag = identity;
